# ============================================================================
# PYTHON PACKAGE REQUIREMENTS FOR RUWE HOLY GHOST CHURCH MANAGEMENT SYSTEM
# ============================================================================
# CRITICAL: This project uses Python 3.12
# 
# To install packages correctly:
#   python3 -m pip install -r requirements.txt
#
# If you get "ModuleNotFoundError" for psycopg2 or Pillow:
#   python3 -m pip install psycopg2-binary --force-reinstall --no-cache-dir
#   python3 -m pip install Pillow --force-reinstall --no-cache-dir
#
# NEVER use "pip install" directly - always use "python3 -m pip install"
# This ensures packages are installed for the correct Python version
# ============================================================================

# Core Django Framework (v5.2.4)
Django==5.2.4
asgiref==3.9.1
sqlparse==0.5.3

# Database Connectors
# psycopg2-binary: PostgreSQL adapter (primary database)
# PyMySQL: MySQL connector (legacy/backup support)
psycopg2-binary==2.9.10
PyMySQL==1.1.1
dj-database-url==3.0.1

# Django REST Framework & Authentication
djangorestframework==3.15.2
djangorestframework_simplejwt==5.5.0
PyJWT==2.9.0

# Django Extensions & Enhancements
django-cors-headers==4.7.0
django-ckeditor-5==0.2.17
django-js-asset==3.1.2

# Image Processing (CRITICAL: Must match Python version)
# If this fails to import, reinstall with --force-reinstall
pillow==11.3.0

# Static File Serving & Production Server
whitenoise==6.9.0
gunicorn==23.0.0

# Utilities & Dependencies
python-dotenv==1.1.0
python-http-client==3.3.7
starkbank-ecdsa==2.2.0
packaging==25.0
setuptools==80.4.0
wheel==0.45.1
